# Inventory scripts

Future scripts in this directory must default to read-only behavior, avoid collecting secret contents, and support sanitized output. Raw host output must be written only to an ignored private path.
